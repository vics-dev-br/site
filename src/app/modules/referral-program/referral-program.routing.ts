import { Route } from '@angular/router';
import { ReferralProgramComponent } from './referral-program.component';

export const ReferralProgramRoutes: Route[] = [
  {
    path: '',
    component: ReferralProgramComponent
  }
];
