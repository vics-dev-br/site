import { Route } from '@angular/router';
import { CloudCreditsComponent } from './cloud-credits.component';

export const CloudCreditsRoutes: Route[] = [
  {
    path: '',
    component: CloudCreditsComponent
  }
];
